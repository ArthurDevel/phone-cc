# Feature 11: Polish & Deploy -- Implementation Plan

## Overview

Final polish pass: loading states, animations, responsive desktop layout, keyboard handling, haptic feedback, and lint cleanup.

---

## Step 1: Loading States

### Sidebar skeleton rows
**Modify:** `src/components/sidebar.tsx`
- When `loading` is true, show 3 skeleton rows (pulsing gray rectangles) instead of session list

### Chat loading spinner
**Modify:** `src/components/chat-view.tsx`
- Add a `loadingHistory` state to `useChatStream` -- set true while fetching history, false after
- Show centered spinner in message area while loading

### Settings page
**Modify:** `src/app/settings/page.tsx`
- Show skeleton rows while projects are loading

---

## Step 2: Animations

**Modify:** Various components

- Sidebar already has slide-in -- verify `transition-transform duration-200 ease-out`
- Messages: add `animate-in` class (simple CSS animation) on new message bubbles
- Mic button: add `active:scale-95` class
- Tool use modal: add transition classes
- Toast: add slide-in-from-top animation

---

## Step 3: Responsive Desktop Layout

**Modify:** `src/app/page.tsx`, `src/components/sidebar.tsx`

- At `md:` breakpoint (>= 768px):
  - Sidebar always visible (no overlay), fixed width ~280px on left
  - Main content shifts right
  - Remove backdrop on desktop
  - Keep hamburger hidden on desktop

---

## Step 4: Keyboard Handling

**Modify:** `src/components/chat-view.tsx`

- Convert input to textarea that auto-grows up to 4 lines
- `Enter` sends, `Shift+Enter` inserts newline (already done)
- Use `visualViewport` API to keep input visible above mobile keyboard

---

## Step 5: Haptic Feedback

**Modify:** `src/components/chat-view.tsx`

- `navigator.vibrate(50)` on mic button press
- `navigator.vibrate(30)` on message send

---

## Step 6: Build & Lint Cleanup

- Run `pnpm build` -- must pass with zero errors
- Run `pnpm lint` -- fix any issues
- Remove unused imports/variables

---

## Files to Modify

| Action | File |
|--------|------|
| Modify | `src/components/sidebar.tsx` (skeleton, responsive) |
| Modify | `src/components/chat-view.tsx` (loading, animations, textarea, haptic) |
| Modify | `src/app/page.tsx` (responsive layout) |
| Modify | `src/app/settings/page.tsx` (skeleton) |
